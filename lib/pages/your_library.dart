import 'package:flamera/constants.dart';
import 'package:flutter/material.dart';

class YourLibrary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(color: kNavColor),
    );
  }
}
